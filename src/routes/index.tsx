import * as React from 'react'
import { HashRouter, Switch, Route } from 'react-router-dom'

const RouterViewer = () => {
  return (
    <HashRouter>
      <Switch>
        <Route path='/home' c />
      </Switch>
    </HashRouter>
  )
}
